if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    L=max(arr)
    arr.sort()
    for i in range(n):
        if L == max(arr):
            arr.remove(max(arr))
    print(max(arr))
            
